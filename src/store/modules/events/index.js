export const Types = {
  SEARCH_REQUEST: '@events/SEARCH_REQUEST',
};

const INITIAL_STATE = {};

export default function auth(state = { INITIAL_STATE }, action) {
  switch (action.type) {
    default:
      return state;
  }
}
